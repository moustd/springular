plugins {
  id 'java'
}

task buildClient(type: Exec) {
  commandLine 'cmd', '/c', 'ng build client'
}

jar.dependsOn(buildClient)

task testClient(type: Exec) {
  commandLine 'cmd', '/c', 'ng test client'
}

test.dependsOn(buildClient)

jar {
  from '../../dist/client' into 'static'
}
